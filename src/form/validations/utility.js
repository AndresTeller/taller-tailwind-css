export const isCamp = (camp = "") => {
  return camp;
};

export const isNumber = (number = 0) => {
  return !isNaN(number);
};

export const isPositiveNumber = (number = 0) => {
  return number > 0;
};
